<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">🔍 SWI-Prolog MQI Monitoring</h1>
      <p class="mt-2 text-gray-600">
        Real-time monitoring for SWI-Prolog sessions via Machine Query Interface (MQI)
      </p>
      <div class="mt-3 p-3 bg-blue-50 border border-blue-200 rounded-md">
        <p class="text-sm text-blue-800">
          <strong>Note:</strong> This monitoring dashboard currently supports SWI-Prolog MQI sessions only. 
          Other adapters (Erlog, Scryer Prolog) are not monitored yet.
        </p>
      </div>
    </div>

    <!-- Controls -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-8">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">🎛️ Quick Actions</h2>
      
      <div class="flex items-center space-x-4">
        <!-- Auto Refresh Toggle -->
        <div class="flex items-center space-x-3">
          <button
            phx-click="toggle_auto_refresh"
            phx-value-enabled={to_string(not @auto_refresh)}
            class={[
              "relative inline-flex h-6 w-11 items-center rounded-full transition-colors",
              if(@auto_refresh, do: "bg-green-600", else: "bg-gray-200")
            ]}
          >
            <span
              class={[
                "inline-block h-4 w-4 transform rounded-full bg-white transition-transform",
                if(@auto_refresh, do: "translate-x-6", else: "translate-x-1")
              ]}
            />
          </button>
          <span class="text-sm font-medium text-gray-700">
            Auto Refresh (<%= if @auto_refresh, do: "5s", else: "Off" %>)
          </span>
        </div>

        <!-- Action Buttons -->
        <div class="flex space-x-3">
          <button
            phx-click="refresh_stats"
            class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors text-sm"
          >
            🔄 Refresh Now
          </button>
          <button
            phx-click="reset_stats"
            class="px-4 py-2 bg-orange-600 text-white rounded-md hover:bg-orange-700 transition-colors text-sm"
          >
            🗑️ Reset Stats
          </button>
        </div>
      </div>
    </div>

    <!-- Session Statistics -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <!-- Causal Session -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">🔗 Causal Reasoning</h3>
        <%= render_session_stats(@session_stats["Causal"] || %{}) %>
      </div>

      <!-- Constraint Session -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">🧩 Constraint Solving</h3>
        <%= render_session_stats(@session_stats["Constraint"] || %{}) %>
      </div>

      <!-- Playground Session -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">🎯 Prolog Playground</h3>
        <%= render_session_stats(@session_stats["Playground"] || %{}) %>
      </div>
    </div>

    <!-- MQI Session Details -->
    <div class="mt-8 bg-white rounded-lg shadow-md p-6">
      <h3 class="text-lg font-semibold text-gray-900 mb-4">🔧 MQI Connection Status</h3>
      <div class="bg-gray-50 p-4 rounded-md">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
          <div>
            <span class="font-medium text-gray-700">Active Sessions:</span>
            <span class="ml-2 px-2 py-1 bg-green-100 text-green-800 rounded text-xs font-medium">
              <%= length(Enum.filter([@session_stats["Causal"] || %{}, @session_stats["Constraint"] || %{}, @session_stats["Playground"] || %{}], fn stats -> stats["status"] == "active" end)) %> / 3
            </span>
          </div>
          <div>
            <span class="font-medium text-gray-700">Auto Refresh:</span>
            <span class="ml-2 text-gray-900">
              <%= if @auto_refresh, do: "Every 5 seconds", else: "Off" %>
            </span>
          </div>
          <div>
            <span class="font-medium text-gray-700">Last Updated:</span>
            <span class="ml-2 text-gray-900 font-mono text-xs">
              <%= if assigns[:last_update], do: Calendar.strftime(@last_update, "%H:%M:%S"), else: "Never" %>
            </span>
          </div>
        </div>
        
        <div class="mt-4 text-xs text-gray-500">
          <p>This monitoring dashboard tracks SWI-Prolog sessions through the MQI protocol. Statistics are gathered from query execution and include inference counts, timing, and session health.</p>
        </div>
      </div>
    </div>
  </div>
</div>


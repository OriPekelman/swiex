export PHX_HOST=$(echo $PLATFORM_ROUTES | base64 --decode | jq -r 'to_entries[] | select(.value.primary == true) | .key' | sed 's:/*$::' | sed 's|https\?://||')
export SECRET_KEY_BASE="$PLATFORM_ENVIRONMENT_TYPE$PLATFORM_ENVIRONMENT$PLATFORM_PROJECT_ENTROPY"
